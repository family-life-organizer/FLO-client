import React from 'react';
import styled from 'styled-components';
import Calendar from 'react-material-ui-calendar';

const BadgerCalendar = () => {
    return (
        <div>
            <Calendar />
        </div>
    );
}

export default BadgerCalendar;